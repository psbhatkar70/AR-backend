export interface Splitter<T, U> {
    split(chunk: T): U[];
    concat(a: U, b: U): U;
}
export declare function splitChunk<T, U>(splitter: Splitter<T, U>, chunk: T, previous: U | undefined): [U[], U];
export declare const stringSplitter: (separator?: string | RegExp) => Splitter<string, string>;
