declare module "../../async-iterator-stream" {
    interface AsyncIteratorStream<T> {
        traverseBreadth(getChildren: (_: T) => AsyncIterable<T> | AsyncIterator<T, unknown, undefined>): AsyncIteratorStream<T>;
    }
}
export {};
