declare module "../../iterator-stream" {
    interface IteratorStream<T> {
        traverseBreadth(getChildren: (_: T) => Iterable<T> | Iterator<T, unknown, undefined>): IteratorStream<T>;
    }
}
export {};
