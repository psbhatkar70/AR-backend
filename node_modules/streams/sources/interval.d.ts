export interface IntervalScheduler<T = unknown> {
    schedule(interval: number, listener: () => void): T;
    cancel(scheduled: T): void;
}
export declare const defaultScheduler: IntervalScheduler;
export declare function iteratorInterval(periodMillis: number, scheduler?: IntervalScheduler): AsyncGenerator<number, undefined, unknown>;
